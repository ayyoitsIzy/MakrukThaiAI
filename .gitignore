__pycache__/
*.pyc
# Remove it from git tracking (keeps the file locally)
git rm --cached dataset.pt

# Add to .gitignore
echo "*.pt" >> .gitignore

git add .gitignore
git commit -m "Remove dataset.pt from tracking, add to .gitignore"
git push -u origin DataGen